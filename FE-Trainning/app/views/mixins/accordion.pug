mixin accordion(items)
  .accordion(data-accordion)
    each item in items
      - if (typeof(item.show) === 'undefined') item.show = 'false'
      .accordion-title(data-acc-title=item.id)
        span.text!=item.text
        span.icon.icon-arrow-down &nbsp;
      .accordion-content(data-acc-content=item.id, data-is-show=item.show)!=item.content
